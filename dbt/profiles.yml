my_dbt_project:
  outputs:
    dev:
      dbname: destination_db
      host: destination_postgres
      pass: secret
      port: 5432
      schema: dev
      threads: 4
      type: postgres
      user: postgres
  target: dev
